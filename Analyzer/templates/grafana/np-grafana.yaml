{{ if and .Values.grafana.enabled .Values.grafana.ingressEnabled }}
kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: grafana
spec:
  podSelector:
    matchLabels:
      app: grafana
  ingress:
  - {}
{{ end }}
